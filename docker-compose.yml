services:
  users-api:
    build: .
    container_name: users-api-v1
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    env_file:
      - .env
    volumes:
      - users-api-data:/app/src/data

volumes:
  users-api-data: